const api = 'http://api.weatherstack.com';
const APIAccessKey = '';
const apiPath = {
    current: 'current',
    historical: 'historical',
    forecast: 'forecast',
}

module.exports = {
    api,
    APIAccessKey,
    apiPath,
}
